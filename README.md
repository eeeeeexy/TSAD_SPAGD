# TSAD_SPAGD


### Dependencies

- python 3.10, pytorch, numpy, scikit-learn, tqdm

If you have installed above mentioned packages you can skip this step. Otherwise run:

    pip install -r requirements.txt

## Reproduce results

Run:

    python main.py